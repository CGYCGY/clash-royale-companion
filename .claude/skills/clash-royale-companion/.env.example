# Copy to .env in this folder and fill in. .env is gitignored; never commit a real key.
# App base URL, no trailing slash.
CR_COMPANION_URL=https://cr.example.com
# Personal API key from the app's Settings page (starts with crk_).
CR_COMPANION_API_KEY=
